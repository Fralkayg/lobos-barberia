# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env files (copy .env.example instead)
.env
.env.local
.env.*.local

# Excel "database" — generated by `npm run seed`, and real bookings contain
# customer PII (name, phone, email) that shouldn't live in git.
server/data/*.xlsx

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# TypeScript build info
*.tsbuildinfo
